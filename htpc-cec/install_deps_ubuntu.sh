# Run this script inside a Ubuntu distrobox to build the app locally on Bazzite
# This also runs in GitHub Actions to prep the build environment.

sudo apt install \
  python3-dev \
  g++ \
  libcec-dev
